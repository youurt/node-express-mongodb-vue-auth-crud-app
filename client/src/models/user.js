export default class User {
  constructor(username, password, github, twitter, email) {
    this.username = username
    this.password = password
    this.github = github
    this.twitter = twitter
    this.email = email
  }
}
